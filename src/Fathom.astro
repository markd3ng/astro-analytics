---
export interface Props {
  site: string;
  src?: string;
}

const { site, src = 'https://cdn.usefathom.com/script.js' } = Astro.props;
---
<script async defer src={src} data-site={site}></script>
